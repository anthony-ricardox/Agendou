<h1>Tornar-se prestador</h1>

<%= form_with model: @provider, url: provider_path do |form| %>
  <div>
    <%= form.label :bio, "Fale sobre você" %>
    <%= form.text_area :bio %>
  </div>

  <%= form.submit "Confirmar" %>
<% end %>